# 
user.allowCreate && resource._actions = {"read", "update", "create"}

# 
user.allowReload && resource._resourcetype = {"app"} and resource._actions = {"read", "reload"}
user.allowReload && resource._resourcetype = {"app.object"} and resource._actions = {"create", "reload"}

# 
user.allowView && resource._resourcetype = {"app", "app.object"} and resource._actions = {"read"}
